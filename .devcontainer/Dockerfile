# PHP 8.2 + MySQL driver for Laravel (Codespaces)
FROM mcr.microsoft.com/devcontainers/php:8.2

# Install PHP MySQL extension so Laravel can connect to the db service
RUN docker-php-ext-install pdo_mysql
